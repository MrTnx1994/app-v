# تغییرات این نسخه
## نسخه 1.10.19 — 1405/05/11
- **انتقال سیستم تنظیمات پیش‌بینی نیاز انبار به سمت سرور (Server-backed Shared Forecast Settings)**: ارتقای کامل مکانیزم ذخیره‌سازی پارامترهای پیش‌بینی انبار و وضعیت محصولات پنهان‌شده از حافظه محلی مرورگر (`localStorage`) به بانک اطلاعاتی سرور (`app_config` در SQLite). از این پس، تنظیمات «دوره پوشش انبار»، «حاشیه امنیت»، «بازه میانگین‌گیری فروش» و «کالاهای پنهان‌شده» به صورت متمرکز و با استفاده از مکانیزم Debounce بهینه‌سازی شده در بانک اطلاعاتی سرور ذخیره می‌شوند تا تمامی کاربران تعریف‌شده در سیستم به یک مجموعه تنظیمات مشترک و یکپارچه دسترسی داشته باشند.

## نسخه 1.10.18 — 1405/05/11
- **ذخیره‌سازی و ماندگاری تنظیمات پیش‌بینی نیاز انبار (Persistent Stock Forecast Settings)**: پیاده‌سازی مکانیزم ذخیره‌سازی محلی خودکار (`localStorage`) برای تمامی پارامترهای تنظیمی بخش پیش‌بینی انبار. از این پس، مقادیر «دوره پوشش انبار هدف» (coverageDays)، «حاشیه امنیت بحرانی» (safetyDays) و «تعداد روزهای میانگین‌گیری فروش» (averageDays) به صورت کاملاً زنده ذخیره شده و در بازبینی‌های بعدی و روزهای آینده بدون تغییر باقی خواهند ماند، مگر اینکه کاربر صراحتاً مایل به تغییر آن‌ها باشد.

## نسخه 1.10.17 — 1405/05/11
- **قابلیت پنهان‌سازی و بازگردانی پویای محصولات در لیست پیش‌بینی**: اضافه شدن دکمه پنهان‌سازی (آیکون چشم خاموش) به انتهای تمام ردیف‌های جدول پیش‌بینی تولید انبار جهت پنهان‌سازی موقت یا دائم محصولات غیرضروری. همچنین ذخیره‌سازی لیست اقلام پنهان‌شده در حافظه مرورگر (`localStorage`) به همراه طراحی پنل مدیریت پیشرفته و شکیل در بالای جدول برای بررسی، نمایش مجدد انتخابی یا لغو پنهان‌سازی دسته‌جمعی اقلام. محصولات پنهان‌شده به طور خودکار از محاسبات آماری کلی نیز حذف می‌شوند.

## نسخه 1.10.16 — 1405/05/11
- **تغییر واژه «خرید» به «تولید» و افزودن مرتب‌سازی سراسری ستون‌ها**: تغییر تمامی برچسب‌ها، عناوین، کارت‌های آماری، دکمه‌های پرینت و فایل راهنمای محاسباتی بخش پیش‌بینی انبار از «خرید» به «تولید» (نیاز به تولید / برنامه تولید). همچنین افزودن قابلیت پیشرفته و پویا برای مرتب‌سازی تمام ستون‌های جدول (بر اساس الفبای کالا، میزان فروش، میانگین روزانه، موجودی فعلی، روزهای پوشش، حد هدف و پیشنهاد تولید) به صورت صعودی و نزولی با نشانگرهای گرافیکی `ArrowUp` و `ArrowDown`. سیستم به طور پیش‌فرض نتایج جدول را بر اساس **روزهای پوشش فعلی** به صورت صعودی مرتب می‌کند تا اقلام بحرانی ابتدا نمایش داده شوند.

## نسخه 1.10.15 — 1405/05/11
- **طراحی نوین و شکیل نتایج جستجوی مشتریان (Modern Customer Search Results Layout)**: جایگزینی جدول‌های متراکم و پیچیده قبلی با یک ساختار تایم‌لاین کارتی فوق‌العاده زیبا و خوانا. در این نسخه هر نتیجه جستجو به صورت یک کارت مجزا با رنگ‌بندی‌های چشم‌نواز طراحی شده است که اطلاعاتی نظیر تاریخ سفارش، راننده حامل، مقصد سفارش، نوبت توزیع و مجموع وزن (با نشانگر سبز برجسته) را با آیکون‌های هماهنگ نمایش می‌دهد. همچنین اقلام هر سفارش در قالب تگ‌های گرافیکی مجزا با رنگ اختصاصی مشخص شده‌اند تا از سردرگمی کاربر جلوگیری شود.

## نسخه 1.10.14 — 1405/05/11
- **جستجوی هوشمند و پیشرفته‌ی مشتری (Advanced Smart Customer Search)**: افزودن قابلیت فیلترگذاری و جستجوی پیشرفته بر اساس بازه‌های زمانی آماده (مانند ۳۰ روز گذشته، امسال، یک سال گذشته، سال گذشته و از ابتدا)، ماه‌های خاص شمسی (مثل فروردین، تیر، مرداد و...) به تفکیک سال و همچنین محدوده تاریخ دلخواه شمسی به منظور ساده‌سازی فرآیند بررسی سفارش‌های مشتریان. همچنین طراحی رابط کاربری مودال جستجو ارتقا یافته و خلاصه‌ای از شرایط و بازه فعال جستجو به صورت کارت اطلاعات نمایش داده می‌شود.

## نسخه 1.10.13 — 1405/05/11
- **سیستم ورود اختصاصی و ارتقای امنیت مدیران (Custom Login & Admin Enhancement)**: افزودن قابلیت ورود با ایمیل اختصاصی `nazari925@gmail.com` به همراه رمز پیش‌فرض جهت رفع خطای `user-not-found`. همچنین این حساب کاربری به عنوان مدیر ارشد کل سیستم (Primary Admin) ثبت شد تا دسترسی‌های مدیریتی کامل (مانند ریست کارخانه‌ای و مدیریت کاربران) به صورت ایمن برقرار باشد و در بازنشانی کامل سیستم حذف نگردد.

## نسخه 1.10.12 — 1405/05/09
- **دسترسی ویزیتورها به داشبورد اصلی (Visitor Access to Dashboard)**: ارتقای سطح دسترسی حساب‌های کاربری ویزیتور (بازاریاب) به گونه‌ای که بتوانند همانند کاربران عادی داشبورد اصلی و تب‌های مربوطه را با محیط یکپارچه سامانه مشاهده کنند. همچنین فایل قدیمی و جداگانه صفحه ویزیتور حذف گردید.

## نسخه 1.10.11 — 1405/05/09
- **پاکسازی فایل‌های قدیمی و بدون استفاده (Code Cleanup)**: حذف تمامی کدهای قدیمی و کامپوننت‌های بلااستفاده (مانند Navbar, ConfigScreen, WarehouseScreen و...) به منظور سبک‌سازی پروژه و بهبود ساختار کدهای برنامه.

## نسخه 1.10.10 — 1405/05/09
- **هدر و فوتر شیشه‌ای (Glassmorphism Header & Footer)**: شفاف کردن پس‌زمینه هدر و فوتر و استفاده از افکت تاری (Backdrop Blur) تا طیف رنگی زیبای پس‌زمینه در سراسر برنامه به صورت یکپارچه و مدرن دیده شود.

## نسخه 1.10.9 — 1405/05/09
- **افزایش غلظت رنگ پس‌زمینه (Increased Background Color Intensity)**: عمیق‌تر و غلیظ‌تر کردن طیف رنگ‌های پاستیلی پس‌زمینه برای ایجاد جلوه بصری شاداب‌تر و برجسته‌تر در هر دو حالت روشن و تاریک.

## نسخه 1.10.7 — 1405/05/09
- **پس‌زمینه طیف پاستیلی خیره‌کننده (Gorgeous Pastel Gradient Background)**: طراحی طیف رنگی پاستیلی بسیار زیبا، لطیف و زنده از راست به چپ جهت ایجاد جلوه بصری فوق‌العاده حرفه‌ای و جذاب در پس‌زمینه برنامه.

## نسخه 1.10.6 — 1405/05/09
- **طیف رنگی نرم پس‌زمینه (Gradient from Right to Left)**: افزودن گرادینت و طیف رنگی بسیار نرم و مدرن از راست به چپ در پس‌زمینه کل برنامه‌ها در حالت‌های روشن و تاریک برای جلوه بصری زیباتر و چشم‌نوازتر.

## نسخه 1.10.5 — 1405/05/09
- **رفع کامل هنگ و کندی زوم و اضافه شدن ۲۰+ مشتری و سفارش**: حذف کامل تمام ویژگی‌های سنگین رندر، ترنزیشن‌های انیمیشنی جدول، بهینه‌سازی محاسبات و عملکرد سلول‌ها جهت اجرای کاملاً روان و آنی گرید برنامه‌ریزی و فروش حتی با تعداد بالای مشتریان و محصولات و عملیات زوم سریع.

## نسخه 1.10.4 — 1405/05/09
- **بهینه‌سازی کامل کارایی و روان‌سازی جدول برنامه‌ریزی و فروش**: حذف انیمیشن‌ها و ترنزیشن‌های سنگین (`transition-all`) از سلول‌ها و ورودی‌های گرید جهت رفع کامل کندی و هنگ هنگام زوم کردن، انتخاب سلول‌های متعدد (Drag Selection) و تایپ سریع اعداد.

## نسخه 1.10.3 — 1405/05/09
- **تفکیک و ماژولار کردن معماری اپلیکیشن**: خرد کردن فایل حجیم `App.tsx` و جداسازی بخش‌ها به کامپوننت‌های مستقل (`Navbar.tsx`، `DashboardScreen.tsx`، `DriversScreen.tsx`، `WarehouseScreen.tsx`، `WaybillScreen.tsx`، `ConfigScreen.tsx` و مودال‌ها) جهت بهبود خوانایی، کارایی، نگهداری آسان‌تر کد و جلوگیری از تک‌صفحه‌ای بودن سنگین.

## نسخه 1.10.2 — 1405/05/09
- **روشن‌تر شدن صفحه لاگین**: کاهش تیرگی پس‌زمینه و لایه‌های رویی (تنظیم گرادینت و اوپاسیتی پنل‌ها و پس‌زمینه برای افزایش ۳۰ درصدی روشنایی و جذابیت بصری صفحه ورود).

## نسخه 1.10.1 — 1405/05/09
یه بررسی کامل روی نسخه‌ای که خودتون/ابزار دیگه‌ای آماده کرده بود انجام دادم (فایلی که آپلود کردید، `VERSION` توش هنوز 1.9.0 مونده بود ولی `CHANGES_FA.md` خودش گفته بود 1.10.0 — این ناهماهنگی هم همینجا اصلاح شد). این باگ‌های واقعی پیدا و رفع شدن:

- **باگ بحرانی (جستجوی مشتری برای ویزیتور اصلاً کار نمی‌کرد)**: فرانت‌اند به `/api/visitor/customers` درخواست می‌زد که اصلاً روی سرور تعریف نشده بود (فقط `/api/search/customer` وجود داشت، با پارامتر `name` نه `search`). هر جستجویی برای ویزیتور با خطا مواجه می‌شد. رفع شد: الان همه از همون یک endpoint واقعی استفاده می‌کنن؛ سرور خودش بر اساس نقش کاربر تصمیم می‌گیره پاسخ کامل بده یا خلاصه.
- **باگ بحرانی‌تر (مودال جستجو برای ویزیتور اصلاً نمایش داده نمی‌شد)**: چون صفحه‌ی مخصوص ویزیتور قبل از رسیدن به JSX مودال جستجو return می‌کرد، دکمه‌ی «جستجوی مشتریان» هیچ اتفاقی نمی‌انداخت — از نظر ساختاری قابل‌دسترس نبود، حتی اگه endpoint هم درست بود. رفع شد: مودال به یه تابع مشترک تبدیل شد که هم از صفحه‌ی اصلی هم از صفحه‌ی ویزیتور صدا زده می‌شه.
- **نشتی امنیتی**: پاسخ جستجوی مشتری برای ویزیتور، اسم آخرین راننده رو هم برمی‌گردوند و توی جدول نشون می‌داد — دقیقاً همون اطلاعاتی که این نقش نباید ببینه. حذف شد؛ به‌جاش لیست محصولات خریداری‌شده (بی‌خطر و مفیدتر) نمایش داده می‌شه.
- **مشکل کارایی**: توی جستجوی مشتری، لیست محصولات به‌جای یک‌بار در ابتدای درخواست، برای *هر فاکتور* از دیتابیس دوباره خونده و پارس می‌شد. رفع شد (حالا فقط یک‌بار).
- **باگ احتمالی زوم جدول**: بین این نسخه‌ها زوم از `zoom` (که چیدمان صفحه رو درست به‌روز می‌کنه) به `transform: scale` تغییر کرده بود. `transform` باعث می‌شه اسکرول افقی جدول (لازم برای دیدن فاکتورهای زیاد) با محتوای واقعی هماهنگ نباشه — یا محتوا بریده می‌شه یا فضای خالی عجیب می‌مونه. برگردوندیم به `zoom`.
  ⚠️ **صادقانه بگم**: توی همون فایل تغییرات قبلی نوشته شده بود که این تغییر به `transform` برای رفع یه مشکل دیگه (کشیده شدن سلول‌ها به چپ در RTL) انجام شده. چون امکان تست بصری واقعی روی مرورگر رو ندارم، اگه بعد از این تغییر همون مشکل RTL برگشت، حتماً بگید تا با دقت بیشتری (و شاید با کمک عکس/اسکرین‌شات از شما) درستش کنیم.

**همه‌ی موارد بالا (به‌جز زوم که نیاز به تأیید بصری داره) با تست واقعی روی سرور در حال اجرا تأیید شدن — نه فقط خوندن کد.**


- **رفع باگ زوم در جدول برنامه‌ریزی**: استفاده از `transform: scale()` با `transform-origin: top center` و `display: inline-block` برای جلوگیری از کشیده شدن سلول‌ها به چپ و خالی ماندن سمت راست صفحه.
- **اصلاح باگ نرمال‌سازی اعداد در نام راننده**: تبدیل اعداد انگلیسی به فارسی با offset صحیح (`+1776` به جای `+1728`) انجام شد تا تطابق نام راننده‌های دارای عدد به‌درستی انجام شود.
- **بهبود کارایی گرید**: استفاده از `useMemo` برای محاسبات سنگین و استخراج `EditableProductCell` به کامپوننت مجزا.
- **تاریخ پیش‌فرض در جستجوی مشتری**: با کلیک روی دکمه «جستجوی کلی مشتری»، تاریخ امروز به‌طور خودکار در فیلد تاریخ قرار می‌گیرد.
- **قابلیت تنظیم تعداد روزهای میانگین‌گیری در پیش‌بینی نیاز انبار**: کاربر می‌تواند تعداد روزهای گذشته برای محاسبه میانگین فروش را انتخاب کند (پیش‌فرض ۳۰ روز، محدوده ۷ تا ۹۰ روز).
- **جستجوی مشتریان برای نقش ویزیتور**: دکمه «جستجوی مشتریان» به صفحه ویزیتور اضافه شد. ویزیتورها می‌توانند مشتریان را جستجو کنند و مجموع خرید (کیلوگرم) و تاریخ آخرین خرید را مشاهده کنند، بدون دسترسی به اطلاعات حساس فاکتورها.
- **افزودن API جدید `/api/visitor/customers`**: فقط برای نقش‌های visitor، admin و sales در دسترس است و فقط نام مشتری، مجموع وزن و تاریخ آخرین خرید را برمی‌گرداند.
## نسخه 1.9.0 — 1405/05/04
- **نقش جدید «ویزیتور»** به سیستم اضافه شد — از تب «مدیریت کاربران» قابل انتخابه. این نقش وقتی وارد بشه، فقط یه صفحه‌ی مستقل و ساده می‌بینه: ریز موجودی انبار (با فیلتر دسته‌ی کالا و امکان جابه‌جایی بین تاریخ‌ها)، همون رنگ‌بندی جدیدی که برای «ریز موجودی کامل کل انبار» ساختیم.
- **این محدودیت واقعی و امنیتیه، نه فقط ظاهری**: حتی اگه یه ویزیتور بخواد مستقیم با ابزارهای فنی به API سرور درخواست بزنه، سرور خودش هر درخواستی به فاکتورها، مشتری‌ها، آمار فروش، جستجوی مشتری، لیست کاربران و غیره رو با کد ۴۰۳ رد می‌کنه. تنها چیزی که این نقش می‌تونه ببینه، اسم محصولات و عدد موجودیشونه — هیچ اسم مشتری یا راننده‌ای هیچ‌وقت به مرورگرش نمی‌رسه.
- تست شد: ساخت کاربر ویزیتور، ورود، دسترسی موفق به موجودی، و رد شدن (۴۰۳) از تمام endpointهای دیگه — همه با curl واقعی چک شد.

## نسخه 1.8.0 — 1405/05/03
- **فیلتر دسته‌ی کالا به «ریز موجودی کامل کل انبار» اضافه شد** — می‌تونی فقط یه دسته‌ی خاص (مثلاً فقط بادوم‌ها، یا فقط اسنک‌ها) رو نشون بدی به‌جای همه‌ی محصولات.
- **رنگ و لعاب این جدول کامل عوض شد**: هدر گرادینت آبی-فیروزه‌ای، ردیف‌های راه‌راه، و مقادیر موجودی حالا به‌صورت پیل رنگی نشون داده می‌شن (سبز = موجودی مثبت، قرمز = صفر یا کمتر) به‌جای اعداد ساده‌ی سیاه روی سفید.

## نسخه 1.7.0 — 1405/05/03
- **بازطراحی بصری کارت‌های تب رانندگان**: نوار رنگی بالای هر کارت متناسب با رنگ اختصاصی راننده، آیکون badge با گرادینت رنگی، نام راننده بزرگ‌تر و پررنگ‌تر.
- **نوار درصد ظرفیت بارگیری اضافه شد** — این عدد از قبل محاسبه می‌شد ولی هیچ‌جا نمایش داده نمی‌شد؛ حالا با نوار رنگی (سبز/کهربایی/قرمز بسته به درصد پر بودن) نشون داده می‌شه.

## نسخه 1.6.0 — 1405/05/03
- **دکمه‌ی «چاپ همه‌ی برگه‌های خروج»** به بالای تب «عملکرد و مسیر رانندگان» اضافه شد — همه‌ی برگه‌های بارگیری/خروج رانندگان رو با یک کلیک باز می‌کنه برای چاپ، جدا از دکمه‌ی «چاپ تکی حواله»ی هر کارت. این دکمه فقط برای نقش‌های ادمین و مدیر فروش نمایش داده می‌شه؛ راننده‌ها نمی‌بینتش.

## نسخه 1.5.3 — 1405/05/03
- رنگ ۴ کارت داشبورد نظارت («تعداد کل مشتریان ارسالی امروز»، «تعداد رانندگان ارسالی امروز»، «برترین رانندگان»، «تعداد کل سفارشات مشتریان») یکسان شد (فیروزه‌ای/cyan).

## نسخه 1.5.2 — 1405/05/03
- متن پیش‌فرض (placeholder) داخل ورودی‌های نام کاربری و رمز عبور صفحه‌ی لاگین حذف شد.

## نسخه 1.5.1 — 1405/05/03
- بج تاریخ/احوال‌پرسی از پایین پنل برندینگ به بالای صفحه (بالای لوگو) منتقل شد.
- خط توضیح زیر «خوش اومدی 👋» حذف شد.

## نسخه 1.5.0 — 1405/05/03
- **عکس واقعی محصولات به‌عنوان پس‌زمینه‌ی صفحه‌ی لاگین**: از همون عکس صفحه‌ی اصلی سایت خودتون (کاسه‌ی آجیل روکش‌دار + گوجه/فلفل/لیمو) استفاده کردم — بریده‌شده و کمی محو، به‌عنوان پس‌زمینه‌ی محیطی کل صفحه، و با یه لایه‌ی رنگی برند (آبی/نیلی) روی پنل چپ که هم رنگ برند حفظ بشه هم بافت عکس واقعی دیده بشه. عکس داخل خود پروژه (`public/login-hero.jpg`) قرار گرفته، نیازی به اینترنت یا لینک خارجی نداره.
- نکته: چون سایت خودتون اجازه‌ی دریافت خودکار (robots.txt) نمی‌داد، عکس رو از روی اسکرین‌شاتی که خودتون فرستادید برش و پردازش کردم؛ اگه فایل با کیفیت بالاتر و بدون کراپ از عکس اصلی داشته باشید، بفرستید تا جایگزین کنم.

## نسخه 1.4.0 — 1405/05/03
- **پس‌زمینه‌ی سینماتیک صفحه‌ی لاگین**: پنل برندینگ سمت چپ حالا با آیکون‌های دست‌کشیده‌ی بادام‌زمینی، ذرت، تخمه، بادوم و پاکت اسنک تزئین شده که به‌آرومی شناور و می‌چرخن (چندتا واضح جلو، چندتا محو و تار عقب‌تر برای حس عمق سینماتیک) — یه اشاره‌ی به‌جا به محصولات واقعی این کسب‌وکار.
- توضیح داده شد که انقضای هفتگی لاگین از قبل درست تنظیم شده بود (بدون نیاز به تغییر کد).

## نسخه 1.3.0 — 1405/05/03
- **بازطراحی کامل صفحه‌ی لاگین**: چیدمان دو ستونه‌ی جدید (پنل برندینگ با گرادینت و انیمیشن + فرم ورود شیک)، دکمه‌ی نمایش/مخفی‌کردن رمز، انیمیشن‌های ورود نرم، همچنان حالت شب/روز رو حفظ کرده.
- **فیلتر «نوع تغییر» به لاگ فعالیت‌ها اضافه شد**: علاوه بر فیلتر کاربر، حالا می‌تونی لاگ‌ها رو بر اساس دسته (فاکتور، موجودی انبار، مدیریت کاربران، سیستمی/بازنشانی، سایر) هم فیلتر کنی. هر لاگ یه برچسب رنگی داره که دسته‌ش رو نشون می‌ده، برای اسکن سریع‌تر چشمی.
- **صفحه‌بندی لاگ‌ها روی حداکثر ۳۰ مورد در هر صفحه تنظیم شد** (قبلاً ۲۰ بود).
- **قانون نسخه‌گذاری آپدیت شد**: از این به بعد فایل تحویلی با فرمت `app v{VERSION}.zip` (با فاصله) نام‌گذاری می‌شه، و خلاصه‌ی تغییرات هر نسخه علاوه بر `CHANGES_FA.md`، به یه جدول تاریخچه در `README.md` هم اضافه می‌شه.

## نسخه 1.2.1 — 1405/05/03
- **رفع کامل کندی انتخاب سلولی و زوم وقتی فاکتور/مشتری زیاد می‌شه**: علت واقعی این بود که هر تغییر کوچیک (حتی یه پیکسل حرکت ماوس، یا یه کلیک زوم) کل صفحه رو دوباره رندر می‌کرد. حالا حین کشیدن ماوس یا زوم زدن، **هیچ رندر Reactـی اتفاق نمی‌افته** — همه‌چیز مستقیم روی DOM انجام می‌شه (بدون تأخیر، بدون توجه به تعداد فاکتورها). فقط یه رندر سبک در شروع و پایان انتخاب سلول‌ها انجام می‌شه که برای هماهنگی لازمه.
- **دکمه‌ی زوم به گوشه‌ی پایین-چپ صفحه چسبید** (`fixed`) — دیگه لازم نیست برگردی بالای صفحه؛ هر جای جدول که باشی در دسترسه.

## نسخه 1.2.0 — 1405/05/02
تب «پشتیبان‌گیری» یه بخش «منطقه خطر» جدید گرفت، **فقط قابل مشاهده و استفاده برای مدیر اصلی سیستم** (`admin@system.com`) — هیچ کاربر دیگه‌ای، حتی با نقش ادمین، نه می‌بینتش نه ازش استفاده می‌کنه (چک هم سمت فرانت هم سمت سرور انجام می‌شه):

- **پاک کردن برنامه‌های روزانه و لاگ‌ها**: همه‌ی فاکتورهای تمام تاریخ‌ها + تاریخچه‌ی لاگ فعالیت پاک می‌شه؛ رانندگان/کالاها/کاربران دست‌نخورده می‌مونن.
- **ریست کامل کارخانه‌ای**: همه‌چیز خام می‌شه (رانندگان، کالاها، برنامه‌های روزانه، لاگ‌ها، و همه‌ی کاربران به‌جز مدیر اصلی) — با تایپ عبارت تأیید `RESET` قبل از اجرا، هم سمت فرانت هم سمت سرور چک می‌شه که اتفاقی فعال نشه.

تست شد: کاربر ادمین غیر-اصلی از هر دو عملیات با کد ۴۰۳ رد شد؛ ریست جزئی رانندگان/کالاها رو دست‌نخورده نگه داشت؛ ریست کامل همه‌چیز رو پاک کرد ولی ورود مدیر اصلی با همون رمز قبلی همچنان کار کرد.

## نسخه 1.1.0 — 1405/05/02
- **بازگردانی از فایل‌های قدیمی مستقیماً از رابط کاربری**: دکمه‌ی «بازگردانی از فایل پشتیبان» حالا علاوه بر فرمت بک‌آپ جدید، فایل‌های خام قدیمی `db_store.json` و `db_logs.json` (همون فرمتی که این پروژه قبل از SQLite داشت) رو هم مستقیم می‌شناسه و ادغام می‌کنه. دیگه لازم نیست فایل رو داخل پوشه‌ی پروژه بذارید — همه‌چیز از همون دکمه‌ی آپلود انجام می‌شه.
- تست شد: آپلود مستقیم `db_store.json` خام (۱۲ راننده، ۷۳ محصول، ۶ کاربر، ۶ روز) و `db_logs.json` خام (۱۳ لاگ) — هر دو درست شناسایی و ادغام شدن.

## نسخه 1.0.0 — 1405/05/02
اولین نسخه‌ی شماره‌گذاری‌شده‌ی این پروژه. شماره‌ی نسخه از این به بعد طبق قوانین [`AI_INSTRUCTIONS.md`](./AI_INSTRUCTIONS.md) افزایش پیدا می‌کنه و فایل تحویلی هر بار با اسم `app{VERSION}.zip` داده می‌شه.

## ۰٫۰ رفع کندی انتخاب سلولی + زوم جدول (آخرین آپدیت)
- **علت کند بودن انتخاب سلولی پیدا و رفع شد**: وقتی با ماوس روی چند سلول می‌کشیدید، هر بار که ماوس وارد یه سلول جدید می‌شد بلافاصله کل جدول دوباره رندر می‌شد (چون این برنامه یک کامپوننت بزرگ یکپارچه‌ست). حالا این آپدیت‌ها با `requestAnimationFrame` محدود شدن به حداکثر یک‌بار در هر فریم (~۶۰ بار در ثانیه)، پس دیگه حین کشیدن ماوس هنگ نمی‌کنه.
- **زوم این/اوت روی جدول اصلی اضافه شد**: کنار دکمه‌ی «جستجوی کلی مشتری»، یه کنترل زوم (+/−) هست که فقط جدول رو بزرگ/کوچک می‌کنه (بین ۶۰٪ تا ۱۵۰٪)، بدون اینکه روی بقیه‌ی صفحه اثر بذاره.

## ۰٫۲ شروع بهینه‌سازی موبایل
- هدر صفحه (لوگو، عنوان، اطلاعات کاربر) روی موبایل جمع‌وجورتر و بدون سرریز افقی شد.
- نوار تب‌های بالا فاصله‌ی کمتری روی موبایل می‌گیره.
- **صادقانه بگم:** این برنامه اساساً حول یه جدول اکسل-مانند عریض ساخته شده؛ برای دیدن کامل جدول روی موبایل همیشه اسکرول افقی لازمه (طبیعت این نوع رابط کاربریه، نه باگ). بقیه‌ی بخش‌ها (مودال‌ها، فرم‌ها، جدول‌های کاربران/لاگ/محصولات) از قبل تا حد خوبی ریسپانسیو بودن. اگه بخش خاصی روی موبایل داشتی که اذیت می‌کنه، بگو دقیقاً کجا تا مستقیم روش کار کنم.

## ۰٫۱ پشتیبان‌گیری و بازگردانی دستی (تازه اضافه شد)
تب جدید «پشتیبان‌گیری» (فقط برای ادمین):
- **دانلود پشتیبان کامل**: یه فایل JSON شامل همه‌چیز (رانندگان، کالاها، کاربران، تمام برنامه‌های روزانه، لاگ فعالیت‌ها) روی سیستم خودتون دانلود می‌شه.
- **دانلود بازه‌ی زمانی**: فقط برنامه‌های روزانه‌ی بین دو تاریخ مشخص رو دانلود می‌کنه (برای پشتیبان‌های سبک‌تر و مقطعی).
- **بازگردانی از فایل**: فایل پشتیبان رو آپلود می‌کنید، سیستم اطلاعاتش رو با اطلاعات فعلی **ادغام** می‌کنه — یعنی:
  - فقط رانندگان/کالاها/کاربرانی که وجود ندارن اضافه می‌شن (چیز موجود دست نمی‌خوره).
  - برای هر تاریخ، فقط فاکتورهایی که از قبل نبودن اضافه می‌شن؛ فاکتورهای فعلی شما پاک یا جایگزین نمی‌شن.
  - موجودی دستی (manual stock override) فقط برای کالاهایی که مقدار نداشتن پر می‌شه.
  - اجرای چندباره‌ی بازگردانی روی یک فایل، مشکلی ایجاد نمی‌کنه (تکراری اضافه نمی‌شه).
- تست شد: بازگردانی کامل روی دیتابیس خالی ✅، بازگردانی تکراری (idempotent) ✅، ادغام با داده‌ای که هم‌زمان محلی هم تغییر کرده بود بدون از دست رفتن هیچ داده‌ای ✅.

**نکته:** ذخیره‌سازی برنامه‌های روزانه از قبل هم نامحدود بود (جدول SQLite هیچ‌وقت خودکار پاک نمی‌شه) — محدودیت «۳۰ روز» فقط مخصوص خلاصه‌ی آمار داشبورده (برای نمایش سریع)، نه دیتای واقعی. برای دیدن هر بازه‌ی دلخواه از تاریخچه، از «جستجوی کلی مشتری» یا دانلود پشتیبان بازه‌ی زمانی استفاده کنید.

## ۰٫۲ بررسی ادعای باگ‌های منطقی (که یه ابزار دیگه مطرح کرده بود)
این موارد رو مستقیماً توی کد چک کردم و **هیچ‌کدوم واقعی نبودن**:
- ادعای عدم پاک شدن `driverName` هنگام تغییر نقش کاربر: کد از قبل درست این مورد رو مدیریت می‌کنه (`driverName: role === 'driver' ? driverName : ''`).
- ادعای ناسازگاری محاسبه‌ی کیلو/کارتن در پیش‌بینی انبار: تمام محاسبات پایه بر اساس کیلوگرم انجام می‌شه و کارتن فقط یه مقدار نمایشی مشتق‌شده‌ست؛ ناسازگاری‌ای پیدا نشد.
- ادعای مشکل در `LoginScreen`: این فایل اصلاً چنین منطقی نداره؛ تبدیل نام‌کاربری به ایمیل فقط سمت سرور انجام می‌شه.
- ادعای مشکل در نرمال‌سازی اعداد نام راننده: این تبدیل عمداً برای یکسان‌سازی ارقام فارسی/انگلیسی نوشته شده و مشکلی توش دیده نشد.

## ۰. ویژگی‌های جدید (آخرین آپدیت)
- **جستجوی کلی مشتری**: دکمه‌ی «جستجوی کلی مشتری» در بالای صفحه‌ی برنامه‌ریزی فروش. نام مشتری + تاریخ شروع رو وارد کن، در تمام تاریخ‌های بعد از اون روز جستجو می‌کنه و نتایج (تاریخ، مشتری، راننده، مسیر، مجموع کیلو) رو توی یه پنجره‌ی جداگانه (فقط نمایش) نشون می‌ده.
- **Sum مثل اکسل روی جدول اصلی**: با کلیک و کشیدن ماوس (یا Shift+کلیک) روی چند سلول از جدول تعداد کالا، یه نوار پایین صفحه ظاهر می‌شه که تعداد سلول انتخاب‌شده و مجموعشون رو نشون می‌ده، با دکمه‌ی «پاک کردن» برای خالی کردن همه‌ی سلول‌های انتخاب‌شده با یک کلیک.
- **صفحه‌بندی (Pagination)**: به لیست کاربران، لاگ فعالیت‌ها (این یکی سمت سرور، برای سرعت بیشتر با رشد دیتا)، لیست محصولات و لیست رانندگان اضافه شد.
- **رفع دو باگ که در همین بررسی پیدا شد**: (۱) لاگ فعالیت‌های مدیریت کاربران قبلاً همیشه به اسم «admin» ثبت می‌شد چون از یک کلید قدیمی و حذف‌شده‌ی localStorage می‌خوند؛ الان از کاربر واقعی لاگین‌شده می‌خونه. (۲) ثبت لاگ از فرانت اصلاً درخواستش موفق نمی‌شد چون هدر احراز هویت نداشت؛ الان درست می‌فرسته.

## ۱. امنیت (احراز هویت واقعی)
- همه‌ی روت‌های حساس (`/api/users/*`, `/api/logs/*`, `POST /api/config`, `/api/save`, `/api/load/*`, `/api/statistics/*`) نیاز به توکن معتبر دارن. توکن موقع لاگین صادر می‌شه و ۷ روز اعتبار داره.
- رمزهای عبور با `bcrypt` هش می‌شن. رمز قدیمی ادمین (`010203`) خودکار هش می‌شه، نیازی به کار دستی نیست — **ولی چون قبلاً به‌صورت متنی ساده جایی به اشتراک گذاشته شده بود، حتماً از پنل «مدیریت کاربران» عوضش کنید.**
- `GET /api/users` دیگه رمز کسی رو برنمی‌گردونه و فقط برای ادمین بازه.
- محدودیت تلاش ورود (rate limit) روی صفحه‌ی لاگین.

## ۲. مهاجرت از فایل JSON به دیتابیس واقعی (SQLite)
- دیتابیس از دو فایل `db_store.json` / `db_logs.json` به یک فایل واقعی `data.db` (SQLite) منتقل شد.
- از ماژول داخلی خود Node.js یعنی **`node:sqlite`** استفاده شده، نه پکیج‌هایی مثل `better-sqlite3`، چون `node:sqlite` نیازی به کامپایل native یا دانلود باینری مخصوص سیستم‌عامل نداره و همیشه روی هاست اشتراکی کار می‌کنه. **تنها نیاز: Node.js نسخه‌ی ۲۲.۵ به بالا روی هاست باشه.**
- **مهاجرت کاملاً خودکاره** — لازم نیست کاری بکنید. اولین باری که سرور رو اجرا کنید:
  - اگه `db_store.json` و `db_logs.json` وجود داشته باشن، تمام رانندگان، محصولات، کاربران، برنامه‌های روزانه و لاگ‌های فعالیت خودکار به `data.db` منتقل می‌شن.
  - بعد از مهاجرت موفق، فایل‌های قدیمی حذف نمی‌شن؛ به `db_store.json.migrated-backup` و `db_logs.json.migrated-backup` تغییر نام می‌دن (فقط برای احتیاط، اگه لازم نبود می‌تونید حذفشون کنید).
  - اجراهای بعدی سرور دیگه این کار رو تکرار نمی‌کنن (چون `data.db` از قبل وجود داره).
- **چرا SQL بهتره:**
  - نوشتن و خوندن با تراکنش واقعی (transaction) انجام می‌شه — دیگه نیازی به قفل دستی نیست و ریسک خراب شدن فایل موقع نوشتن هم‌زمان از بین رفته.
  - برنامه‌ی روزانه‌ی هر تاریخ با یه کوئری ایندکس‌شده خونده می‌شه، نه با پارس کردن کل فایل JSON — سریع‌تره، مخصوصاً وقتی تعداد روزها زیاد بشه.
  - چون از حالت WAL استفاده شده، حتی اگه هاست چند پردازش (process) موازی برای برنامه اجرا کنه، دیتابیس درست و بدون تداخل کار می‌کنه — چیزی که با فایل JSON قبلی تضمین‌شدنی نبود.
- **نکته‌ی صادقانه:** فاکتورها (`invoices`) و بازه‌ی موجودی دستی هنوز به‌صورت JSON داخل یک ستون از دیتابیس ذخیره می‌شن، نه کاملاً normalize‌شده در جدول‌های جدا. این یه انتخاب آگاهانه بود: کاربران، سشن‌ها و لاگ‌ها که ساختار ثابتی دارن کاملاً جدول‌بندی شدن، ولی ساختار فاکتورها (که هر کدوم می‌تونه فیلدهای مختلف داشته باشه) رو به همون شکلی که بود نگه داشتم تا منطق پیچیده‌ی محاسبه‌ی موجودی و آمار دست نخوره و ریسک باگ جدید نداشته باشه.

## ۳. Sync خودکار بین چند کاربر (بدون رفرش دستی)
- هر ۸ ثانیه برنامه‌ی روزانه (فاکتورها، موجودی دستی، جدول جستجو) خودکار از سرور بررسی و در صورت تغییر توسط شخص دیگه، به‌روزرسانی می‌شه.
- هر ۱۵ ثانیه آمار داشبورد هم به‌روز می‌شه.
- اگه خودتون وسط ویرایش/تایپ چیزی هستید که هنوز ذخیره نشده، sync خودکار اون رو **پاک نمی‌کنه**؛ فقط وقتی صفحه‌تون «تمیزه» دیتای جدید رو اعمال می‌کنه. اگه هم‌زمان یکی دیگه چیزی عوض کرده باشه، یه پیام می‌بینید که بعد از ذخیره حتماً بررسی کنید.
- وقتی تب مرورگر پس‌زمینه‌ست، polling متوقف می‌شه.

## نصب و اجرا روی هاست اشتراکی

```bash
npm install
npm run build
NODE_ENV=production node dist/server.cjs
```

اگه از cPanel/Passenger با «Setup Node.js App» استفاده می‌کنید، حتماً **نسخه‌ی Node.js رو روی ۲۲ (یا بالاتر) بذارید**، وگرنه `node:sqlite` در دسترس نیست و سرور بالا نمیاد. تنظیمات startup file (`dist/server.cjs`) بدون تغییر کار می‌کنه.

## نکته‌ی امنیتی مهم که تکرار می‌کنم
رمز قبلی ادمین (`010203`) قبلاً جایی به‌صورت متنی ساده به اشتراک گذاشته شده بود. همین الان از پنل «مدیریت کاربران» عوضش کنید و از این به بعد فرضش رو compromised در نظر بگیرید.
