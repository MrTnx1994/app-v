<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://ai.google.dev/static/site-assets/images/share-ais-513315318.png" />
</div>

> ⚠️ **برای هر هوش مصنوعی/دستیاری که روی این پروژه کار می‌کند:** قبل از هر تغییری، حتماً فایل [`AI_INSTRUCTIONS.md`](./AI_INSTRUCTIONS.md) را بخوانید — قوانین نسخه‌گذاری و نام‌گذاری فایل تحویلی آنجاست.
> نسخه‌ی فعلی پروژه در فایل [`VERSION`](./VERSION) نوشته شده است.

# Run and deploy your AI Studio app

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/6adf1560-b0c3-417b-8561-1b51f07e117e

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`

---

## تاریخچه‌ی نسخه‌ها

جدیدترین نسخه بالای جدول قرار می‌گیرد. برای جزئیات کامل‌تر هر نسخه به [`CHANGES_FA.md`](./CHANGES_FA.md) مراجعه کنید.

| نسخه | تاریخ | خلاصه‌ی تغییرات |
|---|---|---|
| 1.10.2 | 1405/05/09 | افزایش ۳۰ درصدی روشنایی و شفافیت صفحه لاگین با تنظیم گرادینت‌ها و لایه‌های پس‌زمینه. |
| 1.10.1 | 1405/05/09 | رفع باگ‌های واقعی که در بررسی کامل کد پیدا شد: جستجوی مشتری ویزیتور اصلاً کار نمی‌کرد (endpoint اشتباه) و مودالش قابل‌دسترس نبود، نشتی نام راننده به ویزیتور حذف شد، مشکل کارایی جستجو رفع شد، زوم به روش ایمن‌تر برگشت. |
| 1.10.0 | 1405/05/06 | جستجوی مشتریان برای نقش ویزیتور (خلاصه، بدون جزئیات حساس فاکتور)، تنظیم تعداد روزهای میانگین‌گیری در پیش‌بینی انبار (۷ تا ۶۰ روز)، تاریخ پیش‌فرض خودکار در جستجوی مشتری، بهبود کارایی جدول با useMemo. |
| 1.9.0 | 1405/05/04 | نقش جدید «ویزیتور» — فقط دسترسی به ریز موجودی انبار (محدودیت واقعی سمت سرور، نه فقط رابط کاربری). |
| 1.8.0 | 1405/05/03 | فیلتر دسته‌ی کالا + رنگ و لعاب جدید (هدر گرادینت، پیل‌های رنگی موجودی) در «ریز موجودی کامل کل انبار». |
| 1.7.0 | 1405/05/03 | بازطراحی بصری کارت‌های تب رانندگان (نوار رنگی، آیکون گرادینتی) + نوار درصد ظرفیت بارگیری. |
| 1.6.0 | 1405/05/03 | دکمه‌ی «چاپ همه‌ی برگه‌های خروج» در تب رانندگان (فقط ادمین/مدیر فروش). |
| 1.5.3 | 1405/05/03 | یکسان‌سازی رنگ ۴ کارت داشبورد نظارت (فیروزه‌ای/cyan). |
| 1.5.2 | 1405/05/03 | حذف متن پیش‌فرض (placeholder) داخل ورودی‌های نام کاربری/رمز عبور در صفحه‌ی لاگین. |
| 1.5.1 | 1405/05/03 | بج تاریخ به بالای صفحه‌ی لاگین منتقل شد؛ خط توضیح زیر «خوش اومدی» حذف شد. |
| 1.5.0 | 1405/05/03 | عکس واقعی محصولات (از سایت خودشون) به‌عنوان پس‌زمینه‌ی صفحه‌ی لاگین، با لایه‌ی رنگی برند روی پنل چپ. |
| 1.4.0 | 1405/05/03 | پس‌زمینه‌ی سینماتیک صفحه‌ی لاگین با آیکون‌های شناور بادام‌زمینی/ذرت/تخمه/بادوم/پاکت اسنک (لایه‌بندی محو-واضح برای حس عمق). |
| 1.3.0 | 1405/05/03 | بازطراحی کامل صفحه‌ی لاگین (دو ستونه، انیمیشن، نمایش/مخفی رمز)؛ فیلتر «نوع تغییر» + برچسب رنگی روی لاگ فعالیت‌ها؛ صفحه‌بندی لاگ‌ها روی ۳۰ مورد در هر صفحه؛ نام‌گذاری فایل از این به بعد `app v{VERSION}.zip` و ثبت تاریخچه در README. |
| 1.2.1 | 1405/05/03 | رفع کندی جدی انتخاب سلولی و زوم در جدول برنامه‌ریزی فروش (حین درگ/زوم دیگه کل صفحه رندر نمی‌شه)؛ دکمه‌ی زوم به پایین-چپ صفحه چسبید. |
| 1.2.0 | 1405/05/02 | افزودن «منطقه خطر» در تب پشتیبان‌گیری (فقط مدیر اصلی): پاک‌سازی برنامه‌های روزانه/لاگ‌ها، و ریست کامل کارخانه‌ای با تأیید تایپی. |
| 1.1.0 | 1405/05/02 | بازگردانی از فایل پشتیبان حالا فرمت‌های قدیمی db_store.json/db_logs.json رو هم مستقیم می‌شناسه و ادغام می‌کنه. |
| 1.0.0 | 1405/05/02 | اولین نسخه‌ی شماره‌گذاری‌شده: احراز هویت با توکن، هش رمز، مهاجرت به SQLite، sync خودکار چندکاربره، جستجوی کلی مشتری، Sum اکسل‌مانند، Pagination، پشتیبان‌گیری/بازگردانی دستی، رفع کندی اولیه‌ی انتخاب سلولی، شروع بهینه‌سازی موبایل. |

