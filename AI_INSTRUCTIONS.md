# AI_INSTRUCTIONS.md — قوانین نسخه‌گذاری این پروژه

**هر مدل هوش مصنوعی (Claude، ChatGPT، Grok، Gemini، Copilot، یا هر دستیار دیگری) که روی این پروژه کار می‌کنه، قبل از هر چیز این فایل رو بخونه و این قوانین رو دقیق رعایت کنه.**

## نسخه‌ی فعلی پروژه

شماره‌ی نسخه‌ی فعلی همیشه توی فایل `VERSION` (در ریشه‌ی پروژه) نوشته شده — همون رو منبع اصلی در نظر بگیر، نه هیچ جای دیگه.
همین عدد باید با فیلد `"version"` توی `package.json` هم یکی باشه.

## فرمت شماره‌گذاری

فرمت: `MAJOR.MINOR.PATCH` (مثلاً `1.2.3`) — استاندارد Semantic Versioning.

بعد از هر دور تغییر، بر اساس نوع تغییر یکی از این‌ها رو انجام بده:

| نوع تغییر | چیکار کن | مثال |
|---|---|---|
| رفع باگ کوچیک، بدون قابلیت جدید | فقط PATCH رو ۱ واحد زیاد کن | `1.0.0` → `1.0.1` |
| قابلیت جدید (بدون خراب کردن چیز قدیمی) | MINOR رو ۱ واحد زیاد کن، PATCH رو صفر کن | `1.0.5` → `1.1.0` |
| تغییر بزرگ/ساختاری/که ممکنه رفتار قبلی رو عوض کنه | MAJOR رو ۱ واحد زیاد کن، MINOR و PATCH صفر بشن | `1.4.2` → `2.0.0` |

## کاری که باید دقیقاً بعد از هر تغییر انجام بشه

1. **فایل `VERSION`** رو با شماره‌ی نسخه‌ی جدید آپدیت کن.
2. **فیلد `version` در `package.json`** رو هم با همون شماره یکی کن.
3. توی **`CHANGES_FA.md`** یه بخش جدید بالای فایل اضافه کن، با همون شماره‌ی نسخه و تاریخ، و خلاصه‌ی تغییرات همون نسخه رو زیرش بنویس (فارسی، خلاصه، شفاف).
4. **همون خلاصه‌ی تغییرات رو به‌عنوان یه ردیف جدید، بالای بخش «تاریخچه‌ی نسخه‌ها» در `README.md` هم اضافه کن** — این فایل جدا از `CHANGES_FA.md` نگه داشته می‌شه چون README اولین جایی‌ه که هر ابزار/توسعه‌دهنده‌ی دیگه باز می‌کنه.
5. فایل نهایی که به کاربر تحویل می‌دی (zip یا هر فرمتی) رو **دقیقاً** با این الگو نام‌گذاری کن:

   ```
   app v{VERSION}.zip
   ```

   مثلاً برای نسخه‌ی `1.2.3`: **`app v1.2.3.zip`** — دقت کن یک فاصله بین `app` و `v` هست، بدون خط تیره یا هیچ چیز اضافه‌ی دیگه.

6. توی پیام آخر به کاربر، اسم دقیق فایل و شماره‌ی نسخه رو واضح بنویس (مثلاً: «فایل `app v1.2.3.zip` آماده شد»)، تا کاربر بین نسخه‌های مختلف گیج نشه.

## چرا این قانون مهمه

کاربر این پروژه ممکنه از چند ابزار/دستیار هوش مصنوعی مختلف (نه فقط این یکی) برای توسعه‌ی این برنامه استفاده کنه. بدون شماره‌گذاری واضح و اسم فایل ثابت و قابل‌پیش‌بینی، تشخیص اینکه کدوم فایل جدیدترینه و چه تغییری باهاش اومده تقریباً غیرممکن می‌شه. رعایت‌نکردن این قانون باعث سردرگمی مستقیم کاربر می‌شه.

## نکته‌ی مهم برای این فایل

این فایل (`AI_INSTRUCTIONS.md`) خودش هم باید همیشه توی ریشه‌ی پروژه باقی بمونه و حذف نشه، چون تنها مرجع این قوانینه.
